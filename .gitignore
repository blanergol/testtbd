# Личные заметки: «не видна команде» значит, что файл не попадает в историю.
private/

# Локальное состояние TeamBrains живёт вне репозитория, но дублируем на всякий случай.
.teambrains/state.db
.teambrains/cache/
*.tmp
.DS_Store
